node_modules
.next
out
build
.DS_Store
*.pem

# local env
.env
.env*.local

# local embedded postgres data directory
.pglite
.pglite-e2e

# The explainer PDF is a build output of docs/groundwork-explained.html.
# Committed deliberately anyway: it is the thing being handed over, and a
# reviewer should not have to build it to read it.

# test + build output
coverage
test-results
playwright-report
*.tsbuildinfo
next-env.d.ts
